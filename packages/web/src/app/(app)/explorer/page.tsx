/**
 * MOVED — /explorer is now served from (marketing)/explorer/page.tsx
 * to share the marketing header/nav.
 *
 * Sub-routes (/explorer/event, /explorer/tx, /explorer/address)
 * remain in the (app) group and keep the app layout.
 *
 * Roger: if the build complains about this file, delete it manually.
 */
export const _moved = true;
