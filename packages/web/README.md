# @kryndel/web — Explorador (Next.js)

Explorador visual de la capa de contratos del XRPL (la "radiografía"):
contrato + ABI legible → lista de llamadas decodificadas → timeline de una llamada
(eventos + tx emitidas + cambios de estado) → botón **"vigilar → alerta"**.

**Estado:** placeholder. Pendiente de scaffold real de Next.js (Etapa 9 del runbook).
Reutiliza la estética del pitch (`04-assets/kryndel-pitch.html`).
